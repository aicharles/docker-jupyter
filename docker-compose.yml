version: '3'
services:

  jupyter:
    build:
        context: .
    environment:
      GRANT_SUDO: "yes"
      JUPYTER_ENABLE_LAB: "yes"
    volumes:
      - $HOME:/home/jovyan/work
    ports:
      - "8888:8888"
    user: root
